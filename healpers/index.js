const { HttpError } = require("./HttpError");
const { CtrlWrapperr } = require("./CtrlWrapperr");
const { sendMail } = require("./sendMail");

module.exports = {
    HttpError,
    CtrlWrapperr,
    sendMail
}